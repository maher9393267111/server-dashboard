const OverallStat = require('../models/overallStats');
const tryCatch = require('../controllers/utils/tryCatch');

const getDashboardStats = tryCatch(async (req, res) => {
    const currentMonth = 'December';
    const currentYear = 2022;
    const currentDay = '2022-12-06';
    // Overall Stats

});

